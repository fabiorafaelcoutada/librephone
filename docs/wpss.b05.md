# wpss.b05

This appears to be a data file. None of the [tools](tools.md) can
identify the architecture. It has a small binary header, but most
of the file is empty. This is probably a memory dump used to allocate
space for dynamic data.

