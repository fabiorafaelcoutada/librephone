# wpss.b10

This appears to be a data file of error messages. It's basically just
an array of strings. For example:

	m.c:[SM:RcvEvt] Evt:%d not handled in CurS:%d mod_id=%x instance=%x vapid =%d make_sm = 0x%x extra = 0x%x extra2 = 0x%x extra3 = 0x%x

	platform_diag_pmon.c:Enabling Module %u does not match module mask: %x
 
